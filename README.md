# E-commerce Sales & Customer Dashboard ✨

Dashboard ini dibuat untuk menampilkan hasil analisis data dari **E-Commerce Public Dataset**.
Analisis mencakup performa penjualan dari waktu ke waktu serta segmentasi pelanggan
menggunakan metode **RFM (Recency, Frequency, Monetary)**.

---

## Setup Environment - Anaconda
```bash
conda create --name main-ds python=3.9
conda activate main-ds
pip install -r requirements.txt